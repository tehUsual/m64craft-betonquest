npcs:
  '34': "maroutil"

conversations:
  maroutil:
    quester: "MaroUtil"
    first: init

    NPC_options:
      init:
        text: "Hello!"
        pointer: p_init,p_cleanup
    
    player_options:
      p_init:
        text: "Initialize"
        event: core-main.init_castle
      p_cleanup:
        text: "Deinitialize"
        event: core-main.reset_castle
